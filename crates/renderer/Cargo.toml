[package]
name = "markdown_to_pdf_renderer"
version = "0.1.0"
edition = "2021"

[dependencies]
minijinja = "2.0"
html-escape = "0.2"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
insta = "1.40"
pretty_assertions = "1.4"
rstest = "0.23"
