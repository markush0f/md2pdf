[package]
name = "markdown_to_pdf_cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "m2p"
path = "src/main.rs"

[dependencies]
markdown_to_pdf_core = { path = "../core" }
markdown_to_pdf_renderer = { path = "../renderer" }
